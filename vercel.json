{
  "buildCommand": "cd web && npm run build",
  "outputDirectory": "web/dist",
  "installCommand": "cd web && npm install",
  "framework": "astro",
  "functions": {
    "api/predict.py": { "runtime": "python3.12" }
  }
}
